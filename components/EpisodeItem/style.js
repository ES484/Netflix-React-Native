import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    episodeContainer: {
        paddingBottom: 10
    },
    image: {
        width: 100,
        height: 100,
        marginRight: 10
    },
});
export default styles;